# Algorithm cross-links are extensionless and resolved at runtime by the
# VitePress router, so they cannot be validated against the static build
# output. Exclude them from the check.
exclude = ['#algo-ref-']
